meta {
  name: Userinfo
  type: http
  seq: 9
}

get {
  url: {{baseurl}}/userinfo
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
